import React from 'react';

const Form = () => {

    const [formData, setFormData] = useState()
    // for submissions, user fills form and submits (http: POST )
    // for updates, form is populated with fetched data, user edits and submits (http: POST.. or PATCH)
    // for delete, proj details page will have edit button, which will include delete option.  
    return (
        <div>

        </div>
    );
};

export default Form;